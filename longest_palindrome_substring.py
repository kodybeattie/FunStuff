#Given a string s, find the longest palindromic substring in s.
#You may assume that the maximum length of s is 1000.
def find_pal(s):
    if len(s)==0 or s.isspace():
        return ("")
    else:
        if (len(s)%2) 
    middle = (len(s))/2
    # start in middle of string
    # if char on left doesnt match char or right
    # not palindrome

    # if char on left does match char on right
    # check next set of characters on left and right of centre

if __name__ == "__main__":

012345678
